# Implementation Strategy

## Approach

Keep the update narrowly scoped:

- Add a single badge line under the README title.
- Add session docs under `docs/sessions/`.
- Avoid source, dependency, contract, and generated artifact changes.

## Git Strategy

Prepare in:

`PlayCua-wtrees/sladge-badge`

This avoids mixing the badge rollout with unrelated canonical checkout changes.
