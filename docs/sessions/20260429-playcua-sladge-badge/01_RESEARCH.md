# Research

## Repo Evidence

The README includes a `ComputerAgent` example using `model="claude-sonnet-4-5"`
to run a desktop automation instruction. That makes the repo materially tied to
AI-agent computer-use workflows.

## Local State

The canonical `PlayCua` checkout had unrelated local Cargo, spec, ADR, and
worklog changes before this pass. The badge was prepared in an isolated
worktree.

## Decision

Add the sladge badge as governance metadata and defer canonical integration
until the unrelated local changes are handled.
