# Session Overview

## Goal

Add the sladge governance badge to `PlayCua` because its README documents a
computer-use agent driven by a Claude model.

## Outcome

- Added the `AI Slop Inside` badge near the top of `README.md`.
- Used isolated worktree `PlayCua-wtrees/sladge-badge`.
- Left canonical `PlayCua` untouched because it already had unrelated Cargo,
  spec, ADR, and worklog changes.

## Success Criteria

- README links to `https://sladge.net`.
- Session docs record the ComputerAgent/Claude rationale.
- Worktree is clean after commit.
