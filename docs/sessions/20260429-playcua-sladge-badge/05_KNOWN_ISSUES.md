# Known Issues

## Open

- The prepared badge commit is not merged into canonical `PlayCua` because that
  checkout already contains unrelated Cargo, spec, ADR, and worklog changes.

## Resolved

- None. This pass is a governance metadata update only.
